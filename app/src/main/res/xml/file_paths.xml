<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- External files directory for camera images -->
    <external-files-path
        name="pictures"
        path="Pictures/" />

    <!-- Internal files directory -->
    <files-path
        name="files"
        path="." />

    <!-- Cache directory for temporary files -->
    <cache-path
        name="cache"
        path="." />
</paths>